---
summary: "CLI reference for `crocbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `crocbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
crocbot uninstall
crocbot uninstall --all --yes
crocbot uninstall --dry-run
```


---
summary: "Historical reference: Apple device model identifier mappings (legacy)"
read_when:
  - Understanding historical device model identifier patterns
---

# Device model database (historical)

This document describes historical patterns for mapping Apple device model identifiers to friendly names. The native app platforms (macOS, iOS) have been removed from active development, but this reference is retained for historical context.

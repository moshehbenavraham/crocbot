# =============================================================================
# OpenClaw Environment Configuration
# =============================================================================
#
# This file documents environment variables that OpenClaw reads at startup.
# Copy to .env and customize as needed.
#
# NOTE: For production deployments, prefer using ~/.openclaw/.env which is
# automatically loaded as a fallback. The CWD .env takes precedence.
#
# Most configuration belongs in ~/.openclaw/openclaw.json (run `openclaw configure`).
# Environment variables here are primarily for path overrides and secrets.
# =============================================================================


# =============================================================================
# STATE & CONFIG PATHS
# =============================================================================
# Override default ~/.openclaw location for state and config files.

# OPENCLAW_STATE_DIR=/path/to/.openclaw
# OPENCLAW_CONFIG_PATH=/path/to/.openclaw/openclaw.json


# =============================================================================
# VOICE-CALL EXTENSION (Optional)
# =============================================================================
# Only needed if using the voice-call extension with Twilio as provider.
# Can also be configured in openclaw.json under plugins.entries.voice-call.config

# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token_here

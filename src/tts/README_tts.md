# src/tts/

Text-to-speech integration — converts agent text responses to audio.

## Purpose

Uses Edge TTS (`node-edge-tts`) to generate speech audio from text, enabling voice responses in Telegram and other channels that support audio.

## Related

- Media pipeline: `src/media/`
- Voice call extension: `extensions/voice-call/`
